Imports CapaAccesoDatos
Imports MyGeneration.dOOdads
' Generated by MyGeneration Version # (1.3.0.3)

Public Class Areas 
    Inherits _Areas
    Dim sEC_Area As String
    Private dtAreas As DataTable
    Public Property lstAreas() As DataTable
        Get
            Return dtAreas
        End Get
        Set(ByVal value As DataTable)
            dtAreas = value
        End Set
    End Property

    Public Function EC_obtArea(ByVal sEIdArea As String) As String
        'Public Function E_obtArea(ByVal sIdArea As String) As Boolean

        Try
            Me.Where.IdArea.Value = sEIdArea
            Me.Where.IdArea.Operator = WhereParameter.Operand.Equal
            If Me.Query.Load Then
                Return Me.Area
            Else
                Return " "
            End If
        Catch ex As Exception
            Throw ex
        Finally
            Where.WhereClauseReset()
        End Try
        'End Function
        'If E_obtArea(sEIdArea) Then
        '    Return Me.Area
        'Else
        '    Return False
        'End If
    End Function
    Public Function EC_ListaAreas() As Boolean
        If LoadAll() Then
            dtAreas = Me.DataTable
            Return True
        Else
            Return False
        End If
    End Function
End Class
